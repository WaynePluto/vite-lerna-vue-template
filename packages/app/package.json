{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run kill-ports && vite --force",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "kill-ports": "kill-port --port 3000"
  },
  "dependencies": {
    "common_lib": "*",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@types/node": "^17.0.35",
    "@vitejs/plugin-vue": "^2.3.3",
    "@vitejs/plugin-vue-jsx": "^1.3.10",
    "kill-port": "^2.0.0",
    "sass": "^1.52.1",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vue-tsc": "^0.34.7"
  }
}